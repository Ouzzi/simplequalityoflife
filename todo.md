## Todo:

- (done) rightclick furnace with lava to fill  → implemented as `enableFurnaceLavaFill` (FurnaceLavaFillHandler)

## Bugs:

- immersive hotbar prevents simplevisuals item held tooltip
  (Hinweis: betrifft die separate Mod "simplevisuals", nicht dieses Repo – hier nicht behebbar)
